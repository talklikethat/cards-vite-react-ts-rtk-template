import React, { ReactElement } from 'react';
import PageCards from './pages/PageCards';

const App = (): ReactElement => {
    return (
        <div className="app">
            <PageCards />
        </div>
    );
};

export default App;
